# Required: Gemini API key (keep this file server-side only)
GOOGLE_API_KEY=your_gemini_api_key_here

# Required: Supabase (server-side only)
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Server port (Render usually injects PORT automatically)
PORT=8080

# CORS: "*" allows any frontend, or set comma-separated domains
# Example: CORS_ORIGINS=https://your-app.vercel.app,https://staging.vercel.app
CORS_ORIGINS=*

# Live proxy WebSocket settings
LIVE_PROXY_PATH=/ws/live
GEMINI_WS_URL=wss://generativelanguage.googleapis.com/ws/google.ai.generativelanguage.v1beta.GenerativeService.BidiGenerateContent

# Optional reliability tuning
HEARTBEAT_INTERVAL_MS=25000
MAX_QUEUE_MESSAGES=120
